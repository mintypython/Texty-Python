///Everything in this object is an example and not required to use Texty Python

//create my texty to draw my special batched text
example_text = texty_create("I'm impatient, so this text types out instantly!");

//type out the whole thing instantly (no typewriter effect)
texty_skip(example_text);